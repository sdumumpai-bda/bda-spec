<!--
═══════════════════════════════════════════════════════════════════════════════
  READ-ONLY · DO NOT EDIT THIS FILE
═══════════════════════════════════════════════════════════════════════════════
  ไฟล์ใน `standards/templates/` เป็น snapshot ของ BDA AI Dev Standard org repo v0.7.0
  จะถูก **overwrite ทั้ง folder** เมื่อรัน `/bda-sync`
  ────────────────────────────────────────────────────────────────────────────
  ต้องการแก้?
    • Project-wide (commit + share team) → คัดลอกไฟล์นี้ไป `templates/<name>.md`
      → แก้ที่นั่น → commit  (lookup priority สูงกว่า standards/)
    • Personal-only (ไม่ commit)         → คัดลอกไป `.bda-spec/local/templates/<name>.md`
      → แก้ที่นั่น (gitignored)         (lookup priority สูงสุด)
  ════════════════════════════════════════════════════════════════════════════
-->

# Obsidian Context Manifest

Default file name: `00-Agent-Context.md`

Default indexes: `sessions/_index.md`, `test-evidence/_index.md`

> ใช้ไฟล์นี้เป็น canonical Obsidian context manifest ให้ command อื่นอ่านก่อนทำงาน เมื่อ project ใช้ Obsidian เป็น knowledge base

## Project

- Project name:
- Vault path:
- Project folder:
- Source repo path:
- Owner/team:
- Last updated:

## Scope

- In scope:
- Out of scope:
- Shared vault/repo write rule:

## Structure Map

- Home/index note:
- Architecture/docs:
- Requirements/specs:
- Sessions/work notes:
- Changelog/release notes:
- Test reports/evidence:
- Decisions/ADR:
- References:

## Naming, Links, And Metadata

- File naming pattern:
- Folder naming pattern:
- Link style:
- Required frontmatter:
- Tags:
- Existing templates:

## Command Integration

- `plan-work`: read this manifest before planning; include target Obsidian note/evidence path in the plan when relevant.
- `fix-bug`: append root cause, fix summary, regression check, and evidence to the active session note.
- `build-feature`: append feature summary, changed files, verification, and evidence to the active session note.
- `write-document`: create/update the selected Obsidian note and preserve links/frontmatter.
- `test-report`: create/update testcase and evidence notes under the configured evidence path.
- `update-obsidian`: reconcile session/evidence/index notes after implementation.

## Current Work Note

- Active session note:
- Active evidence note:
- Related task/issue:

## Verification / Evidence Rules

- Testcase storage path:
- Screenshot/media storage path:
- Command output/log storage path:
- Evidence that must be captured:
- Pending evidence wording:

## Known Risks / Limits

-

## Change Log

-
